{
  "name": "ai-tracker",
  "private": true,
  "workspaces": ["frontend"],
  "scripts": {
    "dev": "node scripts/run-local.mjs dev",
    "start": "node scripts/run-local.mjs start",
    "build": "npm run build --workspace frontend",
    "check": "npm run check --workspace frontend",
    "test": "node --test scripts/run-local.test.mjs && npm run test --workspace frontend"
  }
}
